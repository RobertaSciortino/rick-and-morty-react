function character(id, name, avatar, species, gender, status, originLocation, lastLocation, episode) {
    return {
        id,
        name,
        avatar,
        species,
        gender,
        status,
        originLocation,
        lastLocation,
        episode
    }
}

export default character;